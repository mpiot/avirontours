<form method="post" action="{{ path('logbook_entry_delete', {'id': logbook_entry.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet éléménet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logbook_entry.id) }}">

    {% block buttons %}
        <button class="btn btn-outline-danger btn-sm">
            <span class="fas fa-trash-alt"></span> Supprimer
        </button>
    {% endblock %}
</form>
