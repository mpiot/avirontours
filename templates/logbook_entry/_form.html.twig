{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.shell) }}

    {{ form_row(form.crewMembers) }}

    {{ form_row(form.nonUserCrewMembers) }}

    {{ form_row(form.startAt) }}

    {{ form_row(form.endAt) }}

    {{ form_row(form.coveredDistance) }}

    {{ form_row(form.shellDamages) }}

    <twig:Button variant="primary">{{ button_label|default('Enregistrer') }}</twig:Button>
{{ form_end(form) }}
