<div class="d-flex flex-wrap align-items-baseline gap-2">
    <span class="fs-5">Saison {{ season_category.season.name }}</span>
    <span class="text-muted">&middot;</span>
    <span class="fs-5">{{ season_category.name }}</span>
</div>
