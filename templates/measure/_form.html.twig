{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.measuredAt) }}
    {{ form_row(form.type) }}
    {{ form_row(form.value) }}

    {{ form_rest(form) }}

    <button class="btn btn-primary">{{ button_label|default('Sauver') }}</button>
{{ form_end(form) }}
