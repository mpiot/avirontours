{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.measuredAt) }}
    {{ form_row(form.type) }}
    {{ form_row(form.value) }}

    {{ form_rest(form) }}

    <twig:Button variant="primary">{{ button_label|default('Enregistrer') }}</twig:Button>
{{ form_end(form) }}
