{{ form_start(form) }}
    {{ form_errors(form) }}

    <div class="row">
        <div class="col-sm-6">{{ form_row(form.lightAerobicHeartRateMin) }}</div>
        <div class="col-sm-6">{{ form_row(form.heavyAerobicHeartRateMin) }}</div>
        <div class="col-sm-6">{{ form_row(form.anaerobicThresholdHeartRateMin) }}</div>
        <div class="col-sm-6">{{ form_row(form.oxygenTransportationHeartRateMin) }}</div>
        <div class="col-sm-6">{{ form_row(form.anaerobicHeartRateMin) }}</div>
        <div class="col-sm-6">{{ form_row(form.maximumHeartRate) }}</div>
        <div class="col-sm-6">{{ form_row(form.maximumOxygenConsumption) }}</div>
    </div>

    <twig:Button variant="primary" class="mt-3">Enregistrer</twig:Button>
{{ form_end(form) }}
