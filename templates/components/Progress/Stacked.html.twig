{# Several bars laid end to end, each one a share of the same whole. The parts are `Progress:Segment`. #}
<div{{ attributes.defaults({ class: 'progress-stacked' }) }}>
    {% block content %}{% endblock %}
</div>
