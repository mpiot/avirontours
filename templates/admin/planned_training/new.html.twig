{% extends 'admin/base.html.twig' %}

{% block title %}Nouvel entraînement plannifié{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('training_plan_index') }}">Gestion des plans d'entraînement</a></li>
    <li class="breadcrumb-item"><a href="{{ path('training_plan_show', { 'id': training_plan.id }) }}">{{ training_plan.name }}</a></li>
    <li class="breadcrumb-item active">Nouvel entraînement planifié</li>
{% endblock %}

{% block body %}
    <h1>Nouvel entraînement planifié</h1>

    <div class="card border-0 shadow">
        <div class="card-body">
            {{ include('admin/planned_training/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
