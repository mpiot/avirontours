{% extends 'admin/base.html.twig' %}

{% block title %}{{ training.trainedAt|format_date('short') }} - {{ training.sport.label }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('admin_training_index') }}">Suivi des entraînements</a></li>
    <li class="breadcrumb-item"><a href="{{ path('admin_training_list', { user_id: user.id }) }}">{{ user.fullname }}</a></li>
    <li class="breadcrumb-item active">{{ training.trainedAt|format_date('short') }} - {{ training.sport.label }}</li>
{% endblock %}

{% block body %}
    <h3>
        {{ training.sport.label }}<br>
        <small>{{ training.trainedAt|format_date('short') }}</small>
    </h3>

    {{ include('training/_show.html.twig') }}
{% endblock %}
