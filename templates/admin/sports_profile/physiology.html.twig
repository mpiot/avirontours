{% extends 'admin/base.html.twig' %}

{% block title %}Physiologie de {{ user.fullname }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('sports_profile_index') }}">Gestion des profils sportifs</a></li>
    <li class="breadcrumb-item active">Physiologie de {{ user.fullName }}</li>
{% endblock %}

{% block body %}
    <div class="d-flex">
        <h1>Physiologie de {{ user.fullname }}</h1>
    </div>

    <div class="card border-0 shadow">
        <div class="card-body">
            {{ include('sport_profile/_physiology_form.html.twig') }}
        </div>
    </div>
{% endblock %}
