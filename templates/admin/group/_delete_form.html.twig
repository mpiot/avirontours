<form method="post" action="{{ path('group_delete', {'id': group.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet éléménet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">

    {% block buttons %}
        <button class="btn btn-outline-danger btn-sm">
            <span class="fas fa-trash-alt"></span> Supprimer
        </button>
    {% endblock %}
</form>
